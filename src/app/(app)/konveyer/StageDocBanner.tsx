'use client';

// Hujjat paketi (talabnoma ro'yxati / sud hujjati) QULFLANGAN — UI orqali yuklab/o'chirib bo'lmaydi
// (admin ham). O'zgartirish faqat kod/DB orqali. Shuning uchun bu inline «yuklash» banneri endi
// KO'RSATILMAYDI (dead upload bo'lmasin). Komponent import qilinishi saqlanadi (return null).
import type { AppDocKey } from '@/lib/app-docs';

export function StageDocBanner(_props: { kind: AppDocKey }) {
  return null;
}
